import { StyleSheet } from "react-native";
export default StyleSheet.create({
    label: {
        fontFamily: 'Comfortaa-Bold',
        fontSize: 16,
    },
    container: {
        padding: 12,
        borderRadius: 12,
        alignItems: 'center',  
    },
});